# Finacial_depart_TASK_Nimap

1. install all dependancies using pip install -r requirement.txt
2. run the command --> uvicorn app.main:app --reload
3. open the browser and run http://127.0.0.1:8000/docs
4. Pyhton version 3.11.7


steps:

1. open the link (app)
2. POST-->register
3. POST--> login  its give the token --> copy token
4. Go to Authorize (top right corner)--> give username , password, id, token click Authorize (logout)
5. then perform the operations e.g. upload pdf/ txt
6. Retrive using GET method
